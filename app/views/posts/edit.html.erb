<h1>編集する</h1>

<%= form_with model: @post do |f|%>
   
    <div>
        <%= f.label :content %><br>
        <%= f.text_area :content, :placeholder =>"#{@post.content}" %>
    </div>
    <div><%= f.submit "更新する" %></div>
<% end %>
  <%= link_to "戻る", :back %>