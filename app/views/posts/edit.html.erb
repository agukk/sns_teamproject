<h1>Posts#edit</h1>

<%= form_for(@post, url: post_path(@post)) do |f| %>
   
    <div>
        <%= f.label :content %><br>
        <%= f.text_area :body, :placeholder =>"#{@post.body}" %>
    </div>
    <div><%= f.submit "更新する" %></div>
<% end %>